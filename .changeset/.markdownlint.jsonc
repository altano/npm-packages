{
  // Changeset files are generated by `pnpm changeset`, not hand-written, and
  // never start with a heading. MD041 wants a top-level h1 on the first line,
  // which isn't something we control or want.
  "MD041": false
}
