---
"@altano/repository-tools": patch
---

replace the `find-cache-dir` dev dependency with `find-cache-directory`. `find-cache-dir@6` is deprecated in favor of the renamed package; the API is unchanged.
