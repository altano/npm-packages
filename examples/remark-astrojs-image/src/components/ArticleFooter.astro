---
import Author from "./Author.astro";
import nullthrows from "nullthrows";
import DateTime from "./DateTime.jsx";

export interface Props {
  frontmatter: MarkdownFrontmatter;
}

const { frontmatter } = Astro.props as Props;
const { dateCreated, dateUpdated, description, tags } = frontmatter;
---

<footer>
  {
    dateUpdated && (
      <>
        Last updated <DateTime date={dateUpdated} client:idle /> —{" "}
      </>
    )
  }Originally published <DateTime date={dateCreated} client:idle />
  <Author name="Alan Norbauer" href={nullthrows(Astro.site)} />
</footer>

<style></style>./DateTime.js ./DateTime.js
